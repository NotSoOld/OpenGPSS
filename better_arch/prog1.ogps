fac op("single");
queue op;
mark lol;

/* Program starts here; all blocks work properly */
exitwhen(rejected > 10);
{{
/* write program after double brackets */
:inject("main", 6, 2, 0, 0);
lol:queue_enter(op);
:fac_enter(op);
:queue_leave(op);
:wait(5, 3);
:fac_leave(op);
:travel(lol, 0.7);
:reject(1);
}}
