exitwhen(curticks > 3);
{{
inject("erm", 0,0,0,1) {p1 = 0, p2 = 0};
loop_times(xact.p1, 10);
{
	xact.p2 = 0;
//	output(xact.p1);
//	iter_next();
	while(xact.p2 <= 10);
	{
		output(to_str(xact.p2));
		xact.p2++;
		iter_stop();
	}
	output("\n");
}
reject(1);
}}
